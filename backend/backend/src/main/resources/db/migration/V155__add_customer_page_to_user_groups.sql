-- Customer portal was removed from the product, so this migration is intentionally a no-op.
-- Keeping V155 as a valid script allows Flyway to continue on databases currently at version 154.
SELECT 1;
